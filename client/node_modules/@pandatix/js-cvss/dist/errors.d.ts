export declare class InvalidMetric extends Error {
    version: string;
    metric: string;
    constructor(version: string, metric: string);
}
export declare class InvalidMetricValue extends Error {
    version: string;
    metric: string;
    value: string;
    constructor(version: string, metric: string, value: string);
}
